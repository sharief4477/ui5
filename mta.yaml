_schema-version: "3.2"
ID: cpappui5everything
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: cpappui5everything-destination-content
  type: com.sap.application.content
  requires:
  - name: cpappui5everything-destination-service
    parameters:
      content-target: true
  - name: cpappui5everything-repo-host
    parameters:
      service-key:
        name: cpappui5everything-repo-host-key
  - name: cpappui5everything-uaa
    parameters:
      service-key:
        name: cpappui5everything-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: cpappui5everything_html_repo_host
          ServiceInstanceName: cpappui5everything-html5-service
          ServiceKeyName: cpappui5everything-repo-host-key
          sap.cloud.service: cpappui5everything
        - Authentication: OAuth2UserTokenExchange
          Name: cpappui5everything_uaa
          ServiceInstanceName: cpappui5everything-xsuaa-service
          ServiceKeyName: cpappui5everything-uaa-key
          sap.cloud.service: cpappui5everything
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: cpappui5everything-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cpappui5everything-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cpappui5everything.zip
      name: cpappui5everything
      target-path: resources/
- name: cpappui5everything
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: cpappui5everything-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cpappui5everything-destination-service
    service-plan: lite
- name: cpappui5everything-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: cpappui5everything-xsuaa-service
    service-plan: application
- name: cpappui5everything-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cpappui5everything-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
